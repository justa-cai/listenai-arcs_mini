/**
 ****************************************************************************************
 *
 * Don't modify this file. It is automatically generated by a script.
 *
 * Author: KeHuatao
 *
 * Copyright (C) ListenAI 2025
 *
 ****************************************************************************************
 */
#include <stdio.h>
#include <stdint.h>
#include <string.h>
#include <lwip/opt.h>
#include <lwip/pbuf.h>
#include <lwip/netif.h>
#include "ls_err.h"
#include "ls_wifi_type.h"
#include "net_al.h"
#include "ipc.h"
#include "mrpc.h"
#include "mrpc_lwip_api_msg.h"
#include "mrpc_lwip_api_server.h"

static void mrpc_lwip_pbuf_alloc(void *msg, struct mrpc_resp_msg *resp_msg)
{
    mrpc_lwip_pbuf_alloc_req_t *req;
    mrpc_lwip_pbuf_alloc_resp_t *resp;

    req = (mrpc_lwip_pbuf_alloc_req_t*)msg;
    resp = (mrpc_lwip_pbuf_alloc_resp_t*)resp_msg;
    memset(resp, 0, sizeof(mrpc_lwip_pbuf_alloc_resp_t));

    resp_msg->status = lwip_pbuf_alloc((const struct pbuf **)req->pbuf, req->layer, req->length, req->type);
}

static void mrpc_lwip_pbuf_free(void *msg, struct mrpc_resp_msg *resp_msg)
{
    mrpc_lwip_pbuf_free_req_t *req;
    mrpc_lwip_pbuf_free_resp_t *resp;

    req = (mrpc_lwip_pbuf_free_req_t*)msg;
    resp = (mrpc_lwip_pbuf_free_resp_t*)resp_msg;
    memset(resp, 0, sizeof(mrpc_lwip_pbuf_free_resp_t));

    resp_msg->status = lwip_pbuf_free(req->p, req->count);
}


mrpc_msg_handler_t mrpc_msg_lwip_handlers[MRPC_MSG_ID_LWIP_MAX - MRPC_MSG_ID_LWIP_START] =
{
    mrpc_lwip_pbuf_alloc,  /*MRPC_MSG_ID_LWIP_PBUF_ALLOC*/
    mrpc_lwip_pbuf_free,  /*MRPC_MSG_ID_LWIP_PBUF_FREE*/
    NULL
};