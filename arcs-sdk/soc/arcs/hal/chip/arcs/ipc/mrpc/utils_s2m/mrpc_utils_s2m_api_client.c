/**
 ****************************************************************************************
 *
 * Don't modify this file. It is automatically generated by a script.
 *
 * Author: KeHuatao
 *
 * Copyright (C) ListenAI 2025
 *
 ****************************************************************************************
 */
#include <stdint.h>
#include <string.h>
#include "ls_err.h"
#include "ls_utils.h"
#include "ipc.h"
#include "mrpc.h"
#include "mrpc_utils_s2m_api_msg.h"
#include "mrpc_utils_s2m_api_server.h"

int ls_read_temp_voltage(float * vout)
{
    mrpc_ls_read_temp_voltage_req_t req;
    mrpc_ls_read_temp_voltage_resp_t resp;

    if (sizeof(mrpc_ls_read_temp_voltage_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_LS_READ_TEMP_VOLTAGE;
    if (mrpc_msg_send(&req, sizeof(mrpc_ls_read_temp_voltage_req_t), &resp))
        return LS_FAIL;
    *vout = resp.vout;

    return resp.hdr.status;
}

int8_t ls_get_mac_from_nvs(uint8_t mac_addr[6])
{
    mrpc_ls_get_mac_from_nvs_req_t req;
    mrpc_ls_get_mac_from_nvs_resp_t resp;

    if (sizeof(mrpc_ls_get_mac_from_nvs_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_LS_GET_MAC_FROM_NVS;
    if (mrpc_msg_send(&req, sizeof(mrpc_ls_get_mac_from_nvs_req_t), &resp))
        return LS_FAIL;
    memcpy(mac_addr, resp.mac_addr, sizeof(resp.mac_addr));

    return (int8_t)resp.hdr.status;
}

int8_t ls_get_mac_customized(uint8_t mac_addr[6])
{
    mrpc_ls_get_mac_customized_req_t req;
    mrpc_ls_get_mac_customized_resp_t resp;

    if (sizeof(mrpc_ls_get_mac_customized_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_LS_GET_MAC_CUSTOMIZED;
    if (mrpc_msg_send(&req, sizeof(mrpc_ls_get_mac_customized_req_t), &resp))
        return LS_FAIL;
    memcpy(mac_addr, resp.mac_addr, sizeof(resp.mac_addr));

    return (int8_t)resp.hdr.status;
}

