/**
 ****************************************************************************************
 *
 * Don't modify this file. It is automatically generated by a script.
 *
 * Author: KeHuatao
 *
 * Copyright (C) ListenAI 2025
 *
 ****************************************************************************************
 */
#include <stdint.h>
#include <string.h>
#include "ls_err.h"
#include "ls_wifi_type.h"
#include "ipc.h"
#include "mrpc.h"
#include "spiflash.h"
#include "mrpc_flash_if_api_msg.h"
#include "mrpc_flash_if_api_server.h"

int32_t flash_if_init(FLASH_DEV * dev, unsigned char ud0, unsigned char ud1)
{
    mrpc_flash_if_init_req_t req;
    mrpc_flash_if_init_resp_t resp;

    if (sizeof(mrpc_flash_if_init_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_INIT;
    req.dev = *dev;
    req.ud0 = ud0;
    req.ud1 = ud1;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_init_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int32_t flash_if_read(int32_t offset, void * data, size_t len)
{
    mrpc_flash_if_read_req_t req;
    mrpc_flash_if_read_resp_t resp;

    if (sizeof(mrpc_flash_if_read_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_READ;
    req.offset = offset;
    req.data = data;
    req.len = len;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_read_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int32_t flash_if_write(int32_t offset, const void * data, size_t len)
{
    mrpc_flash_if_write_req_t req;
    mrpc_flash_if_write_resp_t resp;

    if (sizeof(mrpc_flash_if_write_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_WRITE;
    req.offset = offset;
    req.data = (void *)data;
    req.len = len;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_write_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int32_t flash_if_erase(int32_t offset, size_t size)
{
    mrpc_flash_if_erase_req_t req;
    mrpc_flash_if_erase_resp_t resp;

    if (sizeof(mrpc_flash_if_erase_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_ERASE;
    req.offset = offset;
    req.size = size;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_erase_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int32_t flash_if_write_protection_set(bool enable)
{
    mrpc_flash_if_write_protection_set_req_t req;
    mrpc_flash_if_write_protection_set_resp_t resp;

    if (sizeof(mrpc_flash_if_write_protection_set_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_WRITE_PROTECTION_SET;
    req.enable = enable;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_write_protection_set_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int flash_if_security_read(off_t offset, void * data, size_t len)
{
    mrpc_flash_if_security_read_req_t req;
    mrpc_flash_if_security_read_resp_t resp;

    if (sizeof(mrpc_flash_if_security_read_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_SECURITY_READ;
    req.offset = offset;
    req.data = data;
    req.len = len;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_security_read_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int flash_if_security_write(off_t offset, const void * data, size_t len)
{
    mrpc_flash_if_security_write_req_t req;
    mrpc_flash_if_security_write_resp_t resp;

    if (sizeof(mrpc_flash_if_security_write_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_SECURITY_WRITE;
    req.offset = offset;
    req.data = (void *)data;
    req.len = len;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_security_write_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

int flash_if_security_erase(off_t offset)
{
    mrpc_flash_if_security_erase_req_t req;
    mrpc_flash_if_security_erase_resp_t resp;

    if (sizeof(mrpc_flash_if_security_erase_req_t) > IPC_MSG_BUFFER_SIZE)
        return LS_FAIL;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_SECURITY_ERASE;
    req.offset = offset;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_security_erase_req_t), &resp))
        return LS_FAIL;

    return resp.hdr.status;
}

uint8_t flash_if_check_security_support(void)
{
    mrpc_flash_if_check_security_support_req_t req;
    mrpc_flash_if_check_security_support_resp_t resp;

    req.hdr.id = MRPC_MSG_ID_FLASH_IF_CHECK_SECURITY_SUPPORT;
    if (mrpc_msg_send(&req, sizeof(mrpc_flash_if_check_security_support_req_t), &resp))
        return LS_FAIL;

    return (uint8_t)resp.hdr.status;
}

