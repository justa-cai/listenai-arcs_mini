# 项目可根据需要在此文件中设置对应的配置
CONFIG_ARCS_AP_CORE=y
CONFIG_TEST_UNITY=y
CONFIG_LISA_DEVICE=y
CONFIG_LISA_DEVICE_DEBUG=y

# 手动初始化模式配置（可选）
# 启用后，lisa_device_init() 不会在系统启动时自动调用
# 需要在测试代码中手动调用 lisa_device_init()
# 适用于需要精确控制设备初始化时机的测试场景
CONFIG_LISA_DEVICE_MANUAL_INIT=y