if (DEFINED CONFIG_LITE_DAC OR DEFINED CONFIG_LITE_DAC_PROXY)
    listenai_library_named(lite_dac)
    if(CONFIG_LITE_DAC_PROXY)
        listenai_library_sources(lite_dac_proxy.c)
    else()
        listenai_library_sources(lite_dac.c)
    endif()
    listenai_include_directories(.)
endif()
