if (DEFINED CONFIG_LITE_ADC OR DEFINED CONFIG_LITE_ADC_PROXY)
    listenai_library_named(lite_adc)
    if(CONFIG_LITE_ADC_PROXY)
        listenai_library_sources(lite_adc_proxy.c)
    else()
        listenai_library_sources(lite_adc.c)
    endif()
    listenai_include_directories(.)
endif()
