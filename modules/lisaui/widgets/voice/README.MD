# 说明

此目录下为语音模块的用户界面组件，`lisa_ui_scr_*`开头的组件表示此组件占用整个屏幕，

1. 所有的`lisa_ui_scr_*`开头的组件都必须继承与`lisa_ui_scr_base`，
2. 所有的`lisa_ui_scr_*`开头的组件被设计为可的处理当前屏幕下的简单事件(不涉及到具体业务的事件)，
3. 所有的`lisa_ui_scr_*`开头的组件涉及到业务逻辑的事件,需抛出事件给交互层处理，
