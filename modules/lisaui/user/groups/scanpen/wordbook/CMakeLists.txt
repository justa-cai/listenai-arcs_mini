# 添加所有源文件到主库

set(${TARGET_NAME}_GROUP_WORDBOOK_SOURCES
    group_wordbook.c
)

set(${TARGET_NAME}_GROUP_WORDBOOK_PAGE_SOURCES
    pages/wordbook_main_page.c
)

set(${TARGET_NAME}_GROUP_WORDBOOK_WIDGETS_SOURCES
    widgets/lv_wordbook.c
)

target_sources(${TARGET_NAME} PRIVATE
    ${${TARGET_NAME}_GROUP_WORDBOOK_SOURCES}
    ${${TARGET_NAME}_GROUP_WORDBOOK_PAGE_SOURCES}
    ${${TARGET_NAME}_GROUP_WORDBOOK_WIDGETS_SOURCES}
)
